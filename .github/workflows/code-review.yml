name: Code Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get changed files
        id: changed-files
        uses: tj-actions/changed-files@v44

      - name: Review PR
        uses: actions/github-script@v7
        with:
          script: |
            const changedFiles = '${{ steps.changed-files.outputs.all_changed_files }}'.split(' ');
            const additions = ${{ github.event.pull_request.additions }};
            const deletions = ${{ github.event.pull_request.deletions }};
            const changedFilesCount = changedFiles.length;

            let review = [];
            review.push('## 📋 PR 자동 리뷰\n');

            // Summary
            review.push('### 📊 변경 사항 요약');
            review.push(`- 📁 변경된 파일: **${changedFilesCount}개**`);
            review.push(`- ➕ 추가된 줄: **${additions}줄**`);
            review.push(`- ➖ 삭제된 줄: **${deletions}줄**`);
            review.push('');

            // File categorization for docs
            const fileTypes = {
              markdown: [],
              images: [],
              configs: [],
              others: []
            };

            for (const file of changedFiles) {
              if (!file) continue;
              if (file.endsWith('.md')) fileTypes.markdown.push(file);
              else if (file.match(/\.(png|jpg|jpeg|gif|svg)$/i)) fileTypes.images.push(file);
              else if (file.endsWith('.json') || file.endsWith('.yml') || file.endsWith('.yaml')) fileTypes.configs.push(file);
              else fileTypes.others.push(file);
            }

            review.push('### 📂 변경된 파일 분류');
            if (fileTypes.markdown.length) review.push(`- 📝 마크다운: ${fileTypes.markdown.length}개`);
            if (fileTypes.images.length) review.push(`- 🖼️ 이미지: ${fileTypes.images.length}개`);
            if (fileTypes.configs.length) review.push(`- ⚙️ 설정: ${fileTypes.configs.length}개`);
            if (fileTypes.others.length) review.push(`- 📦 기타: ${fileTypes.others.length}개`);
            review.push('');

            // Checklist for docs
            review.push('### ✅ 체크리스트');
            review.push('- [ ] 문서 내용이 정확한지 확인');
            review.push('- [ ] 오타 및 문법 오류 확인');
            review.push('- [ ] 링크가 올바르게 작동하는지 확인');
            review.push('- [ ] 이미지가 올바르게 표시되는지 확인');
            review.push('');

            // Post comment
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number
            });

            const botComment = comments.find(c => c.user.login === 'github-actions[bot]' && c.body.includes('PR 자동 리뷰'));

            if (botComment) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body: review.join('\n')
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: review.join('\n')
              });
            }
